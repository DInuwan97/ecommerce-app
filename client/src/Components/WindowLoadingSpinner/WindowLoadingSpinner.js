import React from 'react';

import classes from './WindowLoadingSpinner.module.css';

const WindowLoadingSpinner = props => {

  return (
    <div className={classes.loader}>Loading...</div>
  );


};

export default WindowLoadingSpinner;